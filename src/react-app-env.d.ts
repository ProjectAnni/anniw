/// <reference types="react-scripts" />

interface MusicIndex {
  catalog: string;
  track: number;
}

interface SiteInfo {
  site_name: string,
  description: string,
  protocol_version: string,
  features: string[]
}